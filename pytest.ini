[pytest]
pythonpath = src
testpaths = tests
# Use a project-local temp root. On some Windows setups the default
# %TEMP%/pytest-of-<user> folder can become inaccessible (PermissionError).
addopts = --basetemp=tmp/pytest-basetemp
